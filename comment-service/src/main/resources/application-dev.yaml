spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog_comment?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ******
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
server:
  port: 8083

logging:
  level:
    com.linshiyi.comment: debug
    org.mybatis: debug
