<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linshiyi.interaction.mapper.LikeMapper">
    <insert id="insert">
        INSERT INTO comment_like (user_id,entity_type,entity_id) VALUES (#{userId}, #{entityType},#{entityId})
    </insert>
    <update id="update">
        UPDATE comment_like SET is_deleted = #{isDeleted} WHERE user_id = #{userId} AND entity_type = #{entityType} AND entity_id = #{entityId}
    </update>
    <select id="selectByCommentIdAndUserIdAndEntityType" resultType="com.linshiyi.interaction.domain.po.Like">
        SELECT * FROM comment_like WHERE user_id = #{userId} AND entity_type = #{entityType} AND entity_id = #{entityId}
    </select>
</mapper>
